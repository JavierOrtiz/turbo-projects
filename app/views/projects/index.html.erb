<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Projects</h1>
  <%= link_to "New project", new_project_path, data: { turbo_frame: dom_id(Project.new) }, class: "btn btn-primary" %>
</div>
<%= turbo_frame_tag Project.new %>
<%= turbo_frame_tag "projects" do %>
  <%= render @projects %>
<% end %>
<%= turbo_frame_tag 'selected_project' %>